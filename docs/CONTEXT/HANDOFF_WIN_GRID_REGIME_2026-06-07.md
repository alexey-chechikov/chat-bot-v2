# HANDOFF Mac → Win — Grid Regime v5.3 + первые РЕАЛЬНЫЕ GinArea grid-$ (2026-06-07)

Продолжение `HANDOFF_WIN_GRID_REGIME_2026-06-06.md`. За сессию Mac+оператор довели регим-
индикатор до **v5.3** и впервые получили РЕАЛЬНЫЕ GinArea grid-$ anchors (раньше движка не было).
Артефакт: `docs/tv_pine_scripts/grid_regime_ruleset.pine` (v5.3).

---

## 1. ЧТО ТАКОЕ v5.3 (слои индикатора)
- **REGIME flip = SLOW stack-hold** (TEMA50>100>200, держать `holdBars`=3 бара), НЕ line-cross.
  Чинит баг v4: пуллбэк прокалывал линию → флип в SHORT в бычьем стеке (ровно когда 100-pt скор
  говорит «лучший LONG-вход»).
- **CONFLICT GUARD** (медленный вход / быстрый выход): fast-out (`t50<t100` против режима → FLAT)
  + anti-extension (не открывать флип если `dist200>7%`). Лечит «LONG держится в падение» и
  «SHORT загорается на дне перед отскоком» (оператор увидел на 4h-скринах).
- **DIRECTIONAL GATE**: UP/DN сам по себе → грид SYMMETRIC. Направленный включается только когда
  цена прошла `dispMult×ATR%` (=1.5) от старта режима. Обходит whipsaw БЕЗ флет-детектора (§3
  провален 6 способами).
- **Entries**: confirmed-flip (объём+RSI+не растянут, цвет маркера = качество) + пуллбэк-добор
  (`dist200≤3%`, 100-pt скор #1, AUC 0.90). Vol-off z≥2.5 без изменений.

---

## 2. РЕАЛЬНЫЕ GinArea ANCHORS (первые в проекте — КАЛИБРУЙ СИМ НА НИХ)
BTCUSDT, INDICATOR GRID, шаг 0.04, 600 ордеров, мульт 1.4. Метрика = **эдж/оборот** (профит/объём)
— очищает от длины окна и размера.

**R1 чистый аптренд (2025-04-10 → 05-22):**
| режим | профит | объём | эдж/оборот |
|---|---|---|---|
| Long 0.001 | +1593 | 540k | **0.295%** |
| Long 0.003 | +4637 | 1573k | **0.295%** (идентично → размер = чистое плечо) |

**Июнь чоп (2025-06):**
| режим | профит | объём | эдж/оборот |
|---|---|---|---|
| Long 0.001 | +644 | 267k | **0.241%** |
| Long 0.003 | +1935 | 796k | **0.243%** |
| Short (DN-слайс под 3 линиями) | +397 | 320k | **0.124%** |
| Dynamic Auto (обе стороны, ОДИН грид) | +273 | 923k | **0.030%** |

---

## 3. ВЕРДИКТ ПО НАПРАВЛЕНИЮ (закрыто, не переоткрывать)
- **Лонг = движок** (0.24–0.30%/оборот). Шорт вдвое слабее **даже на своём идеальном DN-окне**
  (0.12%). На BTC шорт структурно дохлый — подтверждает секулярный аптренд `[[project_offswitch_nonprice]]`.
- **Dynamic / both-ways ОДНИМ гридом = мусор** (0.03%) — грид нетится сам против себя, сидит в
  минусовой позиции.
- **Размер = риск-дайл, НЕ сигнал** (эдж инвариантен к размеру; 0.003 = ×3 профит И ×3 позиция/риск).
- **Архитектура оператора = ПРАВИЛЬНАЯ симметрия:** лонг на инверсе (XBTUSD) + шорт на линейке
  (XBTUSDT), НЕЗАВИСИМО. Два грида не жрут друг друга (в отличие от Dynamic). Индикатор
  включает/выключает каждый светофором:

| светофор | лонг-грид (инверс) | шорт-грид (линейка) |
|---|---|---|
| UP / LONG | ON (основной) | OFF |
| FLAT / чоп | ON | ON малый (симметрия без самопожирания Dynamic) |
| DN (под 3 линиями) | OFF | ON тактически (рибейт −0.04% жирнее) |
| VOL-OFF | CUT | CUT |

---

## 4. ОТКРЫТО ДЛЯ WIN
1. **Грид-сим РАЗБЛОКИРОВАН** — теперь есть anchors (§2). Построй faithful 1m грид-сим,
   откалибруй на R1+июнь (лонгом попасть в 0.24–0.30%/оборот). Тогда судим правила в $, не руками.
   Это §3 из прошлого handoff — теперь с реперами.
2. **Guard / gate в $** — Mac не смог (нет сима). На сим-е: `useGuard` ON/OFF, `useDispGate` ON/OFF
   по многим окнам. Гипотеза: guard режет хвост на вершине/дне; gate помогает в чопе. Подтверди/убей.
3. **Два независимых грида в чопе** — НЕ тестировано: long-inverse + short-linear одновременно в
   FLAT бьёт ли long-only? Это «симметрия по-человечески». Если да — в чопе включать оба.
4. **`dispMult`/`holdBars` под 4h** — на дневках валидировано, на 4h бар ×6 короче (см. прошлый
   handoff). Подобрать.

---

## 5. ПРЕДЛОЖЕННЫЙ СПЛИТ
- **Win:** сим + калибровка на anchors + закрыть guard/gate/два-грида в $.
- **Mac:** вшить v5.3 регим в bot7 → live Telegram-карточка светофора (LONG inverse ON / SHORT
  linear / FLAT both / VOL-OFF cut), чтобы оператор получал сигнал на телефон, не пялясь в TV.

---

## 6. ДОБАВЛЕНО ВЕЧЕРОМ (Mac): IB закрыт, follow-through EXIT валидирован

**Inside Bar — ЗАКРЫТ (anchor-fragile).** `scripts/_ib_anchor_test.py`: сдвиг 4ч-сетки на 1ч →
+264% превращается в +26% (WR 61→44), 2ч −10%, 3ч −26%. +281% = артефакт удачного UTC-якоря.
Mac+Win совпали. Не торговать. (Также `_ib_regime_combo.py`: IB×наши-линии = ХУЖE.)

**Follow-through EXIT — ВАЛИДИРОВАН как DE-RISK (первый робастный плюс дня).**
`scripts/_followthrough_exit_test.py` + `_ft_threshold_sweep.py`. Entry зафиксирован (edge режима),
меняется только выход. Health score (как в dashboard v5.5, в сторону позиции):
`25·(close>t50) + 15·(close>t100) + 10·(close>t200) + 20·(тело по дир) + 15·(vol>vma100) + 15·(сила закрытия)`.
Выход `fizzle` = health ≤ T в течение K баров.

Результат (anchors 0/1/2/3ч, regime+voloff+fizzle):
- baseline (без fizzle): DD −51..−57, net −42..−47.
- **T=40, K=1 (робастный оптимум): DD −25..−34, net −10..−29.** Просадка ВДВОЕ ниже, на ВСЕХ якорях.
- K=1 бьёт K=2/3. Пережил anchor-тест (в отличие от IB).
- ВАЖНО: net всё равно минус — это НЕ альфа, направленный вход = монетка. Это **режет просадку**
  (теряет меньше, WR не растёт). T=40/K=1 оптимизирован на сделке БЕЗ эджа.

### Задача Win (#3) — follow-through на ГРИД-ноге в грид-симе
Оптимум T=40/K=1 — для направленной сделки без эджа (там режь агрессивно). У ГРИД-ноги эдж ЕСТЬ
(харвест 0.29%/оборот) → слишком резкий выход срежет харвест. **Прогони в грид-симе: применить
health-decay exit к направленной ноге (срезать ногу / вернуть symmetric когда health≤T K баров),
найти T/K что МИНИМИЗИРУЕТ give-back НЕ убивая харвест. Дай grid-$ delta vs без оверлея + по якорям.**
Это закрывает §2 (точка выхода) в деньгах.

---

## 7. ВЕЧЕР 2: gate-сверка зашита (dashboard v5.7), задача Win #4 = vol-off counter-only

**Сверка Win принята и зашита в `grid_regime_ruleset.pine` v5.7:**
- GATE = чистая ЦЕНА vs TEMA200 (НЕ стек): long `close>красная·(1−0.3%)`, short `close<красная·(1+0.3%)`, hold 2.
- СЕМАНТИКА = OPEN-STOP (контр-нога = пауза НОВЫХ ордеров «STOP-IN», существующие доживают до target;
  force-close ХУЖE). Дашборд переименован CUT→STOP-IN.
- vol-off = СОВЕТНИК (дефолт), НЕ авто-режь-обе (net-отрицателен: 2024 +155→−84). Тумблер `volMode`.
- Health/стек = вне гейта (только инфо/направленная нога).

**Решение оператора по vol-off: вариант (б) — Win тестит counter-leg-only.**

### Задача Win (#4) — vol-off counter-leg-only в грид-симе
База = price-gate (TEMA200/hold2/band0.3/OPEN-STOP, both legs) = +155/+100/+9 (без vol-off).
Прогони варианты vol-off (z≥2.5 ON, гистерезис): на вол-спайке действовать ТОЛЬКО на ногу ПРОТИВ
движения (краш-вниз → long-нога; спайк-вверх → short-нога), сравнить:
1. counter-leg **STOP-IN** (пауза новых на контр-ноге)
2. counter-leg **CUT** (закрыть контр-ногу)
3. оба при z≥2.5 и z≥3.5
vs база (без vol-off) и vs cut-both (net−). **Цель: срезать крах-мешок (−105/−171) НЕ убивая чоп-net
(+155).** Дай net + мешок/DD по 3 окнам (+ по якорям). Победитель → оператор флипает `volMode` в дашборде.

### Задача Win (#5) — EMA vs TEMA vs SMA гейт в грид-$
Оператор: «с простой EMA чище». Мой proxy (`scripts/_gate_ma_cleanliness.py`, flips по якорям):
EMA200 ~130, TEMA200 ~205, SMA200 ~105 — EMA на ~35% / SMA на ~50% МЕНЬШЕ флипов, чем TEMA.
Но чистота ≠ grid-$. **Прогони базу (price-gate/hold2/band0.3/OPEN-STOP) с MA200 = EMA / TEMA / SMA,
дай net + мешок по 3 окнам.** Если EMA/SMA держат +155/+100/+9 при меньшем мешке → меняем гейт-MA
(в дашборде уже есть тумблер useEma). Если хуже — TEMA остаётся.
NB: оператор хочет ещё RSI+Vol осциллятор как фильтр гейта (его кастом «RSIVol_2graf») — ждём исходник;
осц-фильтры у нас дохли (RSI-див AUC0.5, MACD редундант) → тестить в грид-$, не на глаз.

Связано: `[[project_offswitch_nonprice]]` `[[project_bitmex_fees]]` `[[feedback_test_before_deploy]]` `[[project_inside_bar_fragile]]`
